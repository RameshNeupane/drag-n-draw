let shapeList = []; // stores shapes from database
let objectReferenceList = []; // stores reference of objects of shape classes
let selectedShapeObject = null; // stores seleceted shape with selection box
const selectionBox = new SelectionBox();
